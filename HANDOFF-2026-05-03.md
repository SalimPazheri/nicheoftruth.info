# Handoff - 2026-05-03

## Project

- Site: Niche of Truth
- Repo: `/Users/macbookair/Documents/Codex/2026-04-30/files-mentioned-by-the-user-logiplus/repo-nicheoftruth`
- Live URL: `https://www.nicheoftruth.info/`
- Deployment: GitHub `main` -> Vercel
- Data/auth: Supabase

The site should now be treated as a dynamic Supabase-backed application, not a static-only site. Future work should preserve database compatibility, RLS policies, admin-only actions, loading/error states, and multilingual public/admin UI.

## Current Git State

Repo was clean after the latest push.

Latest commits:

- `29672ef` Add staged birth video sequence
- `874c29e` Add birth audio transition
- `d08dfd7` Make Supabase schema rerunnable
- `c060728` Add admin guide number management
- `81bf433` Keep question widget open after handoff
- `fc5f2e1` Add guided question WhatsApp widget
- `7a873f7` Correct Arabic brand title
- `c8be257` Refine ribbon menu typography
- `b907df0` Add homepage ribbon navigation

## Important Files

- `index.html` - homepage, ribbon nav, Ask a Question widget, Birth experience shell
- `feedback.html` - public feedback form
- `responses.html` - admin login, feedback list, guide-number admin table
- `script.js` - translations, feedback submit, admin auth/listing, guide routing, Birth sequence
- `styles.css` - full site styling and transitions
- `supabase-schema.sql` - rerunnable Supabase schema and RLS policies
- `assets/nicheoftruth-logo.png` - logo/favicon
- `assets/quran-malayalam-cover.jpg` - hero/background/feedback banner
- `assets/firstcry.mp3` - Birth audio
- `assets/birth-1.mp4` - Birth video

## Supabase

The user ran the updated SQL successfully after `supabase-schema.sql` was made rerunnable.

Tables expected:

- `feedback_entries`
- `guide_numbers`

Important policies:

- Anonymous visitors can insert feedback.
- Authenticated users can read feedback.
- Anonymous visitors can read only active guide numbers.
- Authenticated users can read/insert/update/delete guide numbers.

Admin page:

- URL: `https://www.nicheoftruth.info/responses.html`
- Shows feedback entries after login.
- Shows Guide Numbers admin widget below feedback list.
- Guide phone numbers are normalized to digits only, so `+91 81578 15279` saves as `918157815279`.

Do not put admin credentials in code or handoff files.

## Feedback Flow

Feedback form requires:

- Name
- Mobile/WhatsApp
- District
- Feedback text

On submit:

- Saves to Supabase `feedback_entries`.
- Keeps local backup in `localStorage`.
- Shows personalized thanks message.
- Attempts to close the page after success.

## Ask a Question Flow

Homepage Ask a Question opens an in-page widget. It collects:

- Language
- Name
- Mobile/WhatsApp
- Location
- Profession
- Intention
- Question
- Consent checkbox

Then it routes to WhatsApp using active guide numbers from Supabase `guide_numbers`.

Fallback guide remains in `script.js`:

- Main Guide: `918157815279`

The widget stays open after WhatsApp opens and shows a Continue on WhatsApp link.

## Multilingual Support

Language dropdown supports:

- English
- Hindi
- Malayalam
- Arabic

Arabic brand title was corrected to:

- `مشكاة الحق`

Arabic switches page direction to RTL.

## Homepage Design State

Homepage has:

- Logo image with subtle 3D shadow
- Quran blended hero background from left side
- Right-aligned ribbon nav:
  - Home
  - Birth
  - Death
  - Purpose of Life
  - Revert
- Tagline: `Guiding to Salvation`
- Primary CTA remains `Leave Quran Feedback`
- Secondary CTA opens Ask a Question widget

## Birth Experience

Clicking `Birth` now runs a staged in-page sequence:

1. `firstcry.mp3` starts at very low volume.
2. Audio fades up slowly over about 9 seconds.
3. Homepage gradually dims.
4. `birth-1.mp4` fades in and plays muted.
5. After the video ends, the Birth reflection panel fades in.
6. Close button, Escape key, or backdrop click stops and resets the sequence.

Local verification passed:

- Birth click opens staged sequence.
- Video fades in.
- Birth panel appears after video.
- Browser console showed no errors.

Live checks passed:

- `https://www.nicheoftruth.info/index.html` returned `200`.
- `https://www.nicheoftruth.info/assets/firstcry.mp3` returned `200`.
- `https://www.nicheoftruth.info/assets/birth-1.mp4` returned `200`.

## Notes For Next Session

- Continue building as a dynamic app: add tables/policies first when adding new data features.
- If adding admin features, put them behind Supabase auth in `responses.html` or a future dedicated admin page.
- If adding public content pages for Death/Purpose/Revert, follow the Birth pattern only if a cinematic sequence is desired; otherwise consider normal routed sections or database-driven content.
- Keep all four languages updated when adding visible text.
- After code changes, run `node --check script.js`, browser-test the changed flow, commit, push, and verify live URLs.
